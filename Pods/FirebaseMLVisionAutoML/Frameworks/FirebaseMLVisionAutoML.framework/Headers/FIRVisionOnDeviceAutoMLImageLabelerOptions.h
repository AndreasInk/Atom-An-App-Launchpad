#import <Foundation/Foundation.h>

@class FIRAutoMLLocalModel;
@class FIRAutoMLRemoteModel;

NS_ASSUME_NONNULL_BEGIN

/** Options for an on-device image labeler using models generated by AutoML. */
NS_SWIFT_NAME(VisionOnDeviceAutoMLImageLabelerOptions)
DEPRECATED_MSG_ATTRIBUTE(
    "This API is deprecated and replaced by the new standalone ML Kit SDK. See the migration guide "
    "at https://developers.google.com/ml-kit/migration")
@interface FIRVisionOnDeviceAutoMLImageLabelerOptions : NSObject

/**
 * The confidence threshold for labels returned by the image labeler. Labels returned by the image
 * labeler will have a confidence level higher or equal to the given threshold. Values must be in
 * range [0, 1]. If unset or an invalid value is set, the default threshold of 0.5 is used. There is
 * no limit on the maximum number of labels returned by an on-device image labeler.
 */
@property(nonatomic) float confidenceThreshold;

/**
 * Creates a new instance of `VisionOnDeviceAutoMLImageLabelerOptions` with the given `localModel`.
 *
 * @param localModel The AutoML Vision Edge model stored locally on the device. If `nil` is passed,
 *     raises `FIRInvalidAutoMLLocalModel`.
 * @return `VisionOnDeviceAutoMLImageLabelerOptions` instance with the given `localModel`.
 */
- (instancetype)initWithLocalModel:(FIRAutoMLLocalModel *)localModel;

/**
 * Creates a new instance of `VisionOnDeviceAutoMLImageLabelerOptions` with the given `remoteModel`.
 *
 * @discussion It is recommended that the `AutoMLRemoteModel` be downloaded before creating a new
 *     instance of `VisionOnDeviceAutoMLImageLabeler`. To download the remote model, invoke the
 *     `ModelManager`'s `download(_:conditions:)` method and monitor the returned `Progress` and/or
 *     listen for the download notifications defined in `FIRModelDownloadNotifications.h`.
 *
 * @param remoteModel The AutoML Vision Edge model hosted on the server. If `nil` is passed, raises
 *     `FIRInvalidAutoMLRemoteModel`.
 * @return `VisionOnDeviceAutoMLImageLabelerOptions` instance with the given `remoteModel`.
 */
- (instancetype)initWithRemoteModel:(FIRAutoMLRemoteModel *)remoteModel;

/** Unavailable. */
- (instancetype)init NS_UNAVAILABLE;

@end

NS_ASSUME_NONNULL_END
